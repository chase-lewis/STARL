<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.starlabs.h2o">

    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-feature android:name="android.hardware.location.gps" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".controller.LoginUserActivity"
            android:label="@string/app_name" />
        <activity
            android:name=".controller.RegisterUserActivity"
            android:label="@string/title_activity_register" />
        <activity android:name=".controller.InitialActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".controller.MainActivity" />
        <activity android:name=".controller.ViewUserProfileActivity" />
        <activity android:name=".controller.CreateWaterReportActivity" />
        <activity android:name=".controller.ViewWaterReportsActivity"></activity>
    </application>

</manifest>